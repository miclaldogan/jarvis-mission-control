[pytest]
filterwarnings =
    # Ignore httpx TestClient deprecation (will migrate in httpx v0.28+)
    ignore:The 'app' shortcut is now deprecated:DeprecationWarning
    # Ignore FastAPI on_event deprecation (we migrated to lifespan but httpx still warns)
    ignore:on_event is deprecated:DeprecationWarning
