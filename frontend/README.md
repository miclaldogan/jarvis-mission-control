# Deprecated: legacy frontend

This `frontend/` folder is a legacy placeholder.

Use the active UI in:
- [client/](../client)

Recommended dev workflow (from repo root):

```bash
docker compose --profile dev up -d --build backend redis frontend-dev
```

UI runs at `http://localhost:5173`.
