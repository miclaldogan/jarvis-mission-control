## Packages
recharts | For data visualization charts (radar, line, area)
framer-motion | For smooth interface transitions and animations
date-fns | For formatting dates and timestamps

## Notes
Tailwind Config: Extended with custom colors (cyan, electric purple, hud green) and fonts (Fira Code, Inter)
Node: Node.js 18+ required (Node 20 recommended).
API: UI uses real backend endpoints via same-origin `/api/*` (prod nginx) or Vite dev proxy (dev).
